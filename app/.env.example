# Example environment variables for local development.
# Copy this file to `.env.local` and replace placeholder values with real secrets.
# IMPORTANT: Do NOT commit your real `.env.local` containing secrets. `.gitignore` excludes `*.local`.

# -- AI / GenAI API key (recommended: keep server-side only)
# If you must call the GenAI SDK from the browser (not recommended), set VITE_GEMINI_API_KEY so Vite will expose it to the client.
# Prefer: leave GEMINI_API_KEY set for server-side use only and use a server proxy to call the SDK.
# Universal provider selector (gemini | openai | azure | mock)
AI_PROVIDER=gemini

# Universal server-side API key — prefer storing this in CI / server envs, not client builds
AI_API_KEY=

# Optional client-exposed key (ONLY if you must call provider from browser — insecure)
VITE_AI_API_KEY=
VITE_GEMINI_API_KEY=

# Optional client-side control knobs (only used if client reads them via import.meta.env or if vite.config maps them)
# Recommended defaults below — override in .env.local if needed.
VITE_AI_MODEL=gemini-3-flash-preview
VITE_AI_REQUEST_TIMEOUT_MS=180000
VITE_FEATURE_CACHE=true

# Local dev server port
PORT=3000

# Node environment
NODE_ENV=development
