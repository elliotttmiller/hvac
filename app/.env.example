# Example environment variables for local development.
# Copy this file to `.env.local` and replace placeholder values with real secrets.
# IMPORTANT: Do NOT commit your real `.env.local` containing secrets. `.gitignore` excludes `*.local`.

# -- AI / GenAI API key (SERVER-SIDE ONLY for security)
# The server will use this key to make API calls on behalf of the client
# This prevents exposing API keys in browser JavaScript
AI_API_KEY=
GEMINI_API_KEY=
API_KEY=

# -- Server Configuration
# Port for the AI proxy server (default: 4000)
PORT_SERVER=4000

# -- Client Configuration
# Server URL for the client to connect to (default: http://localhost:4000)
VITE_SERVER_URL=http://localhost:4000

# Universal provider selector (gemini | openai | azure | mock)
AI_PROVIDER=gemini

# Optional client-side control knobs
VITE_AI_MODEL=gemini-3-flash-preview
VITE_AI_REQUEST_TIMEOUT_MS=180000
VITE_FEATURE_CACHE=true

# Local dev frontend port
PORT=3000

# Node environment
NODE_ENV=development
